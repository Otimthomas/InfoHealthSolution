// import React from "react";
// import {compose, withProps} from "recompose";
// import {
//   withScriptjs,
//   withGoogleMap,
//   GoogleMap,
//   Marker,
// } from "react-google-maps";

/*
import React from 'react'
import {GoogleMap, withScriptjs, withGoogleMap} from 'react-google-maps';

function Map() {
	return (
		<GoogleMap 
			defaultZoom={18}
			defaultCenter={{ lat: 0.3478, lng: 32.5944 }}
		/>
	);
}

const WrappedMap = withScriptjs(withGoogleMap(Map));


*/

// const Map = compose(
//   withProps({
//     googleMapURL: `https://maps.googleapis.com/maps/api/js?v=3.exp&libraries=geometry,drawing,places&key=AIzaSyC7YMBYQIdrIXK67op0J48iUJG2ZJcv5dM`,
//     loadingElement: <div style={{height: `100%`}} />,
//     containerElement: <div style={{height: `400px`}} />,
//     mapElement: <div style={{height: `100%`}} />,
//   }),
//   withScriptjs,
//   withGoogleMap
// )((props) => (
//   <GoogleMap defaultZoom={18} defaultCenter={{lat: 0.3478, lng: 32.5944}}>
//     {props.isMarkerShown && <Marker position={{lat: 0.3478, lng: 32.5944}} />}
//   </GoogleMap>
// ));

// export default Map;

{
  /* <Map isMarkerShown /> */
}
{
  /* <Map isMarkerShown={false} /> */
}
